import styled from 'styled-components'

export const HeaderContainer = styled.header`
  // background: var(--red-3);
  display: flex;
  justify-content: space-between;
  padding: 4rem 20vw 4rem 20vw;
  // box-shadow: 1px 1px 3px var(--red-4);
`